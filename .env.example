PORT=3000
# Point this to your alpha environment origin (scheme + host[:port])
# Examples:
#   http://localhost:4000
#   https://alpha.nafez.example
ALPHA_ORIGIN=http://localhost:4000

# Base path on the alpha origin where the app is mounted.
# Use '/alpha' for local alpha server; use '/' (or leave empty) when the
# alpha environment is served at the domain root.
# Examples:
#   /alpha
#   /
ALPHA_BASE_PATH=/alpha
